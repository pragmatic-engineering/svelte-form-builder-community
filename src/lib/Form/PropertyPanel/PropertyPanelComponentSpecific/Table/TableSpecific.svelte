<script lang="ts">
	import type { Field } from '$lib/Utils/types';

	export let field: Field;
	// let id = `${LibraryPrefix}-tooltip-${nanoid(10)}`;

	// let fieldInstance = DefinitionManager.getFieldInstance(field);
	// let table: TabulatorFull = fieldInstance.getTable();
	// if (table) {

	// }
</script>
